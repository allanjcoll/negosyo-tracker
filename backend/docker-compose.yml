services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: negosyo-backend
    restart: unless-stopped
    ports:
      - "5000:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
